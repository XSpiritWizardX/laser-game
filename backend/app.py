Define CreateSessionRequest(host_name: str), JoinSessionRequest(player_name: str), SessionResponse(session_id: str, host: str, players: list[str], created_at: datetime). Use a module-level dict keyed by session_id. POST /sessions generates uuid4, stores session with host in players, returns SessionResponse (201). POST /sessions/{session_id}/join returns 404 if missing, 409 if name already in players, otherwise appends and returns SessionResponse. GET /sessions/{session_id} returns 404 if missing, otherwise SessionResponse.
